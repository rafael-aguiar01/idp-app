apiVersion: v1
kind: Service

metadata:
  name: ${{ values.repoName }}

spec:
  type: ClusterIP

  selector:
    app: ${{ values.repoName }}

  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP