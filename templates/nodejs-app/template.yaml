apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: nodejs-app-template
  title: Criar Nova Aplicação Node.js
  description: Cria um novo repositório com o boilerplate Node.js.
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Detalhes da Aplicação
      required: [repoName]
      properties:
        repoName:
          title: Nome do Repositório
          type: string

  steps:
    - id: template
      name: Gerando Estrutura Node.js
      action: fetch:template
      input:
        url: ./skeleton
        values:
          repoName: ${{ parameters.repoName }}

    - id: publish
      name: Publicando Repositório no GitHub
      action: publish:github
      input:
        description: ${{ parameters.description }}
        repoUrl: github.com?repo=${{ parameters.repoName }}&owner=rafael-aguiar01

    - id: register
      name: Registrando no Catálogo
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps.publish.output.repoContentsUrl }}
        catalogInfoPath: '/catalog-info.yaml'